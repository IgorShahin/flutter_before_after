name: flutter_before_after
description: A Flutter package for creating interactive before/after image comparison widgets with drag, zoom, and pan gestures.
version: 1.0.0
homepage: https://github.com/SmartToolFactory/Compose-BeforeAfter

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true