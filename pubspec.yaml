name: before_after_slider
description: Compare before/after images or widgets with a smooth draggable divider, pinch zoom, pan, and customizable overlay UI.
version: 2.0.0
homepage: https://github.com/IgorShahin/before_after_slider
repository: https://github.com/IgorShahin/before_after_slider
issue_tracker: https://github.com/IgorShahin/before_after_slider/issues
topics:
  - image-comparison
  - before-after
  - slider
  - zoom
  - pan

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
