/// Defines where drag can start for moving the before/after slider.
enum SliderDragMode {
  /// Drag starts only when touching the thumb/icon area.
  thumbOnly,

  /// Drag starts from both the divider line and the thumb/icon.
  fullOverlay,
}
